syntax = "proto3";

package api;

option go_package = "grpcTest/api;api";

service Dialog {
    rpc Ask(Request) returns (Response);
}

message Request {
    string question = 1;
}

message Response {
    string answer = 1;
}
